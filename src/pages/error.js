const Error = () => {
    return (
        <div>
            you have been logged out due to an internal server error. Log in
            again. Browse without logging in.
        </div>
    );
};

export default Error;
